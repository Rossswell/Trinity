const fs = require('fs');
// Very simple script to read the image file and get a hex code. We don't have an image parser built-in easily for node, 
// wait, we can just ask the user or guess from typical dark themes. 
